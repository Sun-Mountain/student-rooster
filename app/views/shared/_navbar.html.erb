<a class="skip-nav" href="#main">Skip to main content</a>

<nav class="navbar" id="navigation-bar">
  <%= link_to "Student Rooster", root_path %>

  <div>
    <ul>
      <% if user_signed_in? %>
        <% unless current_user.no_teams? %>
          <li>
            <%= button_to "Students", team_students_url(current_user.team_id), class: 'nav-btn', method: :get %>
          </li>
      <% end %>
        <li>
          <%= button_to "Team Dash", root_path, class: 'nav-btn', method: :get %>
        </li>
        <li>
          <%= button_to "Edit Profile", edit_registration_path(User), class: 'nav-btn', method: :get, name: 'user_edit' %>
        </li>
        <li>
          <%= button_to "Sign Out", destroy_user_session_path, class: 'nav-btn', method: :delete, name: 'user_sign_out' %>
        </li>
      <% else %>
        <li>
          <%= button_to "Login", new_session_path(User), class: 'nav-btn', method: :get %>
        </li>
      <% end %>
    </ul>
  </div>
</nav> 