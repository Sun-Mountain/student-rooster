<a class="skip-nav" href="#main">Skip to main content</a>

<nav class="navbar" id="navigation-bar">
  <%= link_to "Student Rooster", root_path %>

  <div>
    <ul>
      <% if user_signed_in? %>
        <li>
          <%= button_to "Edit Profile", edit_registration_path(User), class: 'btn_sign-out', method: :get %>
        </li>
        <li>
          <%= button_to "Sign Out", destroy_user_session_path, class: 'btn_sign-out', method: :delete %>
        </li>
      <% else %>
        <li>
          <%= button_to "Login", new_session_path(User), class: 'btn_sign-out', method: :get %>
        </li>
        <li>
          <%= button_to "Sign Up", new_user_registration_path, class: 'btn_sign-out', method: :get %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>