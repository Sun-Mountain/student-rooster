<div class="portal-dashboard">
  <% if @user.no_team? %>
    <div class="form-container">
      <div class="form-wrapper">
        <%= form_with model: [ @user, @user.teams.build ] do |form| %>
          <div class="field">
            <%= form.label :name %><br>
            <%= form.text_field :name, class: "field-text" %>
          </p>

          <div class="field">
            <%= form.submit "Create Team", class: "form-button"  %>
          </div>
        <% end %>
      </div>
    </div>
  <% else %>
    YAY!
  <% end %>
</div>